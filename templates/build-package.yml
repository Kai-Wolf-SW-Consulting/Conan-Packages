# Common Conan package build steps template
parameters:
  param: []

steps:
  - ${{ each p in parameters.param }}
    - script:
        conan create -pr $(PROFILE) -r kwc -b outdated -s build_type=$(BUILD_TYPE) ${{ p }} kwc/stable
        conan upload -r kwc --all --confirm ${{ p }}
